import React from 'react'

const Dashboard = () => {
  return (
    <div id="events">
      <h2 className="events-header-h2">Dashboard</h2>
      <div className="event-list">
        {/* {events.map(event => (
          <EventCard key={event.id} event={event} />
        ))} */}
      </div>
    </div>
  );
}

export default Dashboard